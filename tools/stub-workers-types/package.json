{
  "name": "@cloudflare/workers-types",
  "version": "4.99999.0",
  "description": "Local empty stub. Cloudflare Workers runtime types are generated by `wrangler types` into worker-configuration.d.ts. This stub replaces the real @cloudflare/workers-types that drizzle-orm pulls in as an optional peer dependency, which would otherwise declare a duplicate, conflicting set of runtime types and break the global `Env` type.",
  "license": "MIT",
  "types": "index.d.ts"
}
