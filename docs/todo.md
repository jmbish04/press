The frontend is messed up .. the newsstand is showing rows but within the rows are article cards stacked 1 on top of each other vertically, 1 vertical column.

Each row in newsstand should be a horizontal list of articles.

Lets also make a change to make the newsstand rows grouped by source instead of grouping by tag.
